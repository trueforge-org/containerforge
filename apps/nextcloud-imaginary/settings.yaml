# yaml-language-server: $schema=../../settings.schema.json
schema_version: 1
upstream_env_url: "https://nextcloud.com"
ports:
  []
env:
  - name: MALLOC_ARENA_MAX
    default: "2"
    required: false
volumes:
  - path: /config
    required: true
