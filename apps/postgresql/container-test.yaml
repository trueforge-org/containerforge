# yaml-language-server: $schema=../../testhelpers/container-test.schema.json
timeoutSeconds: 120
healthCommands:
- command: pg_isready -h 127.0.0.1 -p 5432 -U ${POSTGRES_USER} -d ${POSTGRES_DB}
tcp:
- port: '5432'
