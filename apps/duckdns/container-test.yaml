# yaml-language-server: $schema=../../testhelpers/container-test.schema.json
timeoutSeconds: 120
commands:
- command: test -x /app/duck.sh
- command: test -f /defaults/logrotate.conf
- command: bash -n /app/duck.sh
- command: head -n 1 /app/duck.sh
  expectedContent: '#!/usr/bin/env bash'
  matchContent: true
