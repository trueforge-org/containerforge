# yaml-language-server: $schema=https://raw.githubusercontent.com/trueforge-org/forgetool/refs/heads/main/schemas/container-settings.schema.json
schema_version: 1
upstream_env_url: "https://github.com/netbox-community/netbox"
ports:
  - port: 8000
    protocol: tcp
    required: false
env:
  []
volumes:
  - path: /config
    required: true
dependencies:
  - name: postgresql
  - name: valkey
