# yaml-language-server: $schema=../../settings.schema.json
schema_version: 1
upstream_env_url: ""
ports:
  []
env:
  - name: TAUTULLI_DOCKER
    default: "True"
    required: false
volumes:
  - path: /config
    required: true
