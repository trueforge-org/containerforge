# yaml-language-server: $schema=../../settings.schema.json
schema_version: 1
upstream_env_url: "https://hub.docker.com/_/node"
ports:
  []
env:
  - name: HOME
    default: "/app/nodehome"
    required: false
volumes:
  - path: /config
    required: true
