# yaml-language-server: $schema=../../settings.schema.json
schema_version: 1
upstream_env_url: "https://nextcloud.com"
ports:
  []
env:
  []
volumes:
  - path: /config
    required: true
dependencies:
  - image: postgresql
  - image: valkey
opt_dependencies:
  - image: memcache
